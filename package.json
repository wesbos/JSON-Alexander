{
  "name": "json-alexander",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && vite build --watch --mode content",
    "build": "vite build --mode content && vite build --mode page-script",
    "zip": "npm run build && node ./scripts/zip-dist.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.2.0",
    "vite-plugin-static-copy": "^3.0.0",
    "yazl": "^3.3.1"
  }
}
